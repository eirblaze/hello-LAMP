{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        // vscode の変数の中身を見るタスク
        // https://github.com/Microsoft/vscode/issues/43867
        {
            "label": "echo workspaceFolder",
            "type": "shell",
            "command":
                "echo ${workspaceFolder}"
        },
        {
            "label": "echo workspaceRoot",
            "type": "shell",
            "command":
                "echo ${workspaceRoot}"
        }
    ]
}